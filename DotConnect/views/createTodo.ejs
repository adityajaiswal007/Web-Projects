<% include partials/header %>
    <div class="model-box" data-aos="flip-left">
        <figure class="box">
            <form action="/todo" method="POST" class="box-text" id="todo-form">
                <textarea name="message" class="todo-box" placeholder="Add Items" required></textarea>
                <button class="enter-button-add">
                    <span>
                    <svg class="side-nav-icon">
                        <use xlink:href="/img/sprite.svg#icon-plus"></use>
                    </svg>
                    </span>
                </button>
            </form>
        </figure>
    </div>
    <script>
        var select = document.getElementById('5')
        select.onload = load();
        function load() {
            select.classList.add('side-nav-item-active');
        }
    </script>  
<% include partials/footer %>